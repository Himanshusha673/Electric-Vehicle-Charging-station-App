// ignore_for_file: constant_identifier_names

enum HomeBottomNavigationItem {
  home,
  history,
  dashboard,
  settings,
}

enum HomeViewType {
  map,
  list,
}

enum PaymentMethodType {
  tokenId,
  card_payment,
  credit_card,
}

enum GoCardLessAccountType {
  sandbox,
  live,
}

enum StripeAccountType { test, live }

enum PayterServersType {
  production,
  test,
  dev,
}
